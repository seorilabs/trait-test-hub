# product-assets

테스트 콘텐츠에 딸린 이미지 자산을 보관합니다.

현재 포함 예정:

- `dpti/type`: DPTI 결과 화면용 유형 이미지
- `dpti/share`: DPTI 공유 preview 이미지

원본은 sibling repo `../dpti-app/public`에서 `scripts/import-dpti-content.mjs`로 가져옵니다.
