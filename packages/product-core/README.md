# product-core

React Native, Firebase, AppsInToss SDK를 import하지 않는 순수 도메인 패키지입니다.

현재 포함 범위:

- 테스트 응답 채점
- 결과 vector와 가장 가까운 결과 선택
- 공개 테스트 fixture

앱 target은 이 패키지 위에 storage, analytics, share, ads, remote config adapter를 붙입니다.
